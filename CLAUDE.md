# 검선귀환 신통 시뮬레이터

> ⚠️ **2026-05-24 진행중 — `데미지공식_진행_2026-05-24.md` 먼저 읽을 것.** 데미지 공식 재구축 중 (Model 2 = 심화가 증가를 곱 증폭 / ÷331 크리분모 / 방어차감 제거 / 신규스탯 / 성해천경 법보전용 / 평타 일반 / 영불씨 호신강기·시체 제외 / 호신강기+6%심화). **[2026-05-24 갱신]** 소명 −31% = 테스트 하네스가 **진무절화(불씨6셋)** 누락 탓이었음(sim 자체는 맞음). 진무절화 넣으니 노크리 풍뢰+5/소명+1/약영−4 전부 ±5% 매치. 남은 것: 붕산 치명 −24%(고cd cMult 부족 — 영불씨는 시체도 적용). 치명 단계 진행 중.


## 프로젝트 구조
- `sim2.js` — 메인 시뮬레이션 엔진. 모든 신통/법보/유파/옵션 로직 포함. 다른 스크립트가 전부 이걸 로드함.
- `rank.js` — 전체 빌드 랭킹 (533개 빌드 × 법보 조합, 순서 휴리스틱 최적화 포함)
- `rank_thunder.js` — 뇌전(청명4) 빌드만 9! 전수탐색 순서 최적화. 빌드당 362,880순열 탐색. 약 26분 소요.
- `optimize.js` — Top 10 빌드 9! 전수탐색
- `trace.js` — 화염 1위 빌드(열산4+청명2) 1회 상세 전투 로그 출력
- `trace_thunder.js` — 뇌전 1위 빌드(청명4+주술2) 1회 상세 전투 로그 출력
- `cal.js` — 인게임 대조 보정 하네스. "🧪 시뮬 셋업" 구성(복룡2/청명2/옥추2 빌드 전체)을
  그대로 세팅해 노치명 8개 대조 포인트 출력. 데미지 공식 보정 작업은 이걸로.

## ⚠️ Git Push 워크플로우 (중요)

이 폴더는 **git 저장소가 아니다**. 별도 git repo와 수동 동기화 후 push.

### 경로
- **로컬 작업 폴더 (현재)**: `C:/Users/ljc47/Pictures/BlueStacks/검선귀환 신통/` ← 여기서 코드 수정
- **Git 저장소**: `C:/Users/ljc47/sword-immortal-sim/` ← 동기화 + commit + push
- **Remote**: `https://github.com/duracen/sword-immortal-sim.git` (origin/main)

### Push 절차
1. 로컬 폴더와 git 저장소의 차이 확인:
   ```bash
   diff -rq "C:/Users/ljc47/Pictures/BlueStacks/검선귀환 신통" "C:/Users/ljc47/sword-immortal-sim"
   ```
2. 변경된 파일을 git 저장소로 복사 (cp). **gitignore된 파일 제외:**
   - `*.docx` (검선귀환_신통_정리.docx 등 — 로컬에서만 재생성)
   - 스크린샷 (`*.png`)
   - 로그 (`*위빌드_상세로그.txt`, `trace_output.txt`, `빌드_랭킹_결과.txt`)
   - 자료 폴더 (`/검/`, `/뇌/`, `/백족/`, `/불씨/`, `/비술/`, `/화/`)
3. `cd /c/Users/ljc47/sword-immortal-sim && git add -A && git commit -m "..." && git push origin main`

### 주의사항
- main 직접 push 가 sandbox 에서 차단될 수 있음 → 권한 다이얼로그에서 Allow
- 개인 프로젝트라 main 직접 push 가 정상 워크플로우 (PR 안 씀)
- commit 메시지 스타일: `sim/UI: [요약]` 또는 `sim: [요약]` (한글 OK)

## 실행 방법
모두 Node.js만 있으면 됨. 의존성 없음.

```bash
node rank_thunder.js          # 뇌전 빌드 9! 전수탐색 (~26분)
node rank.js                  # 전체 빌드 랭킹 (~3분)
node trace.js                 # 화염 1위 상세 로그
node trace_thunder.js         # 뇌전 1위 상세 로그
node optimize.js              # Top 10 전수탐색
```

## sim2.js 수정 시 주의사항
- 원문 docx와 반드시 교차 검증할 것
- **신통 피해 공식**: `effATK × (계수%/100) × 증가감소M × 심화감면M × cMult × fmM`
- **법보 절대값 피해 공식 (`opts.absolute`)**: `원문값 × defMult × 증가감소M × 심화감면M × cMult × fmM`
  - ⚠️ 신통/법보 **둘 다 기준 스탯 전부 적용** — 증가감소M·심화감면M 을 빼면 안 됨 (Claude 가 멋대로 빼서 사용자 분노).
  - 법보 증가감소M: `받는_피해감소_법보`(= 영혼의 불씨 −25%) 반영. 보호막피해보너스(보호막Inc)는 증가합에 포함.
  - 법보 심화감면M: `자기_피해심화_법보` / `받는_피해감면_법보` (기준 스탯) 반영.
  - **법보는 속성 무관 (사용자 확정 2026-05-17)** — 원문에 술법/물리 표기 없음. TREASURES 에서 `attr` 필드 제거됨.
    술법/물리 속성 심화·감면, 속성 세트보너스 **법보엔 미적용** (dealDamage `if (!opts.absolute)` 가드).
  - ❌ 절대 금지: 법보에서 증가감소M·심화감면M·영혼불씨 −25% 를 빼는 수정. 기준 스탯은 전부 공식에 들어간다.
- 작열 DoT는 크리티컬 안 터짐 (dealDotDamage 사용)
- 평타는 95% 감소 적용 (`* 0.05`)
- per-cast 트리거 (일정 시간/횟수 동안 매 신통 cast 마다 자동 발동되는 작열부여/buff 등) 는 SK[name].cast 안이 아니라 main loop 의 pre-cast 훅에서 실행됨. 새 신통/옵션 추가 시 비슷한 패턴은 pre-cast 훅에 등록
- 다단 hit 신통 분기 (sim2.js DECAY 영역) — 사양 키워드에 따라 다른 함수/테이블 사용:
  - "N회 공격, 총 X% 피해" (멀티히트) → `recordEvenHit` 균등 분배 (감폭 X)
  - "N회 반사" → `recordMultiHit` + `REFLECT_DECAY_5`/`REFLECT_DECAY_7` (SKILL_REFLECT 등록, hits = 1+N)
  - "동일 대상 중복 명중 가능" → `recordMultiHit` + `DUPLICATE_HIT_DECAY_3`/`DUPLICATE_HIT_DECAY_4` (SKILL_DUPLICATE_HIT 등록)
  - 그 외 광역 1타 → 단일 `record` / `dealDamage`
- DECAY 테이블은 모두 인게임 raw 데이터로 검증된 값 (CRIT 배율 normalize 후 cast 간 일치). 새 신통 추가 시 SKILL_HITS + 카테고리 Set (SKILL_REFLECT / SKILL_DUPLICATE_HIT) 등록 필수. 새 hit 수 패턴 발견 시 인게임 데이터 받아 cross-check 후 새 DECAY 테이블 추가

---

# 🎯 인게임 내부 테스트 기준값 (사용자 제공 — 강제 사용, 2026-05-17)

⚠️⚠️ 데미지 공식 calibration 은 **이 값으로만**. sim 의 풀강화 placeholder 값 절대 쓰지 말 것.
Claude 가 자꾸 이걸 까먹고 sim 기본값으로 되돌려서 사용자 분노 — **매 작업 시 이 표 먼저 참조.**

## ⚠️⚠️⚠️ 모든 것을 원문(인게임 툴팁 발췌)대로 — 추측·해석 금지 (2026-05-23, 사용자 강력 지시)

**키워드/스탯/효과의 의미는 반드시 원문(게임 내 툴팁 발췌)으로만 확정.** 원문 없이 추측·일반화·
다른 키워드와 혼동하지 말 것. 작업 전 해당 키워드 원문 먼저 Grep. (Claude 반복 실수로 사용자 분노 다회.)

### 미러 = 정확한 복제 클론 (재확인 2026-05-23)
**대상(듀라센) = 플레이어의 정확한 복제 클론. 내가 나를 때리는 것.** 내 스탯 바꾸면 클론도 똑같이 바뀜
(기본 스탯뿐 아니라 모든 옵션값 전부). "고정 더미값"·"기본 스탯만 미러" 아님. Claude 반복 착각 — 재논의 금지.
- **클론은 행동(시전)을 안 하는 패시브 더미.** 내가 선택한 전부(스탯+빌드+법상+불씨+비술+옵션)를 복제하지만, 공격/시전은 안 함.
- **정적 스탯/설정** (영불씨강도·받는감면·방어 등 — 선택으로 정해진 값): 클론이 보유 → 내가 클론 때릴 때 적용 (정상적인 데미지 감소 요소).
- **동적 누적 버프** (위압 등 빙의/행동으로 쌓이는 것): 클론은 행동 안 하니 **0** → 상쇄/감소 없음.
  - 예: 천봉 위압 = "자신 입히는 +3% / 받는 −3% per 겹". 클론이 위압 0이라 클론의 받는 −3% 상쇄가 없음 → **내 위압 입히는 +3%만 순부스트** (sim dealt-only 가 결과적으로 맞음. 2026-05-23 확정).
  - ❌ Claude 가 "클론도 위압 쌓아서 받는 −3% 상쇄"로 잘못 넣었다가 되돌림. **동적 버프는 클론 0 — 상쇄 가정 금지.**


## 🧪 시뮬 셋업 — 인게임 대조 calibration 구성 (⚠️ 이 구성 그대로 강제 사용. 빼먹지 말 것)

⚠️⚠️ 데미지 대조 시뮬 돌릴 때 **아래 8개 항목 전부 세팅.** "버프는 적용하면서
법보·불씨·비술·법상·영역·시전순서·인게임수치를 빼먹는다"고 사용자 다회 분노 —
**매 시뮬마다 이 블록 그대로 구성.** 보정 하네스 = `cal.js` (이미 이 구성으로 세팅됨).

| # | 항목 | 값 |
|---|------|-----|
| 1 | 신통 빌드 | **복룡2 / 청명2 / 옥추2** |
| 2 | 신통 6종 | 청명·풍뢰, 청명·투진, 옥추·소명, 옥추·청사, 복룡·약영, 복룡·붕산 |
| 3 | 공격법보 | 오염혁선 / 경몽비파 / 참원선검 — `attackSetMode:'현명'` (현명3셋: 술법 심화+5%/감면+5%) |
| 4 | 방어법보 | 성해천경 → 경화령도 → 명공현주 (장착·소진 순서) `defenseTreasures` |
| 5 | **불씨** | 통명묘화 3-set (총성급12 → 신통심화 +8%) **+ 진무절화 6-set** (신통 2시전마다 → 다음 신통 입히는피해 +48%; ⚠️소명=3번째·붕산=6번째 신통이 소비 → 이 둘만 +48 받음) — `불씨:{통명묘화:3,진무절화:6,총성급:12}` |
| 6 | **비술** | 탁천마주·무 / 식혼마주·진 / 악신마주·진 — `bisul.self:[{master,branch}×3]` |
| 7 | **법상** | 천봉 (실체+의념+진령 전부) — `법상:{name:'천봉',tiers:{실체:true,의념:true,진령:true}}` (인게임 천봉 사용, 2026-05-18) |
| 8 | **영역** | (미지정 — 이 빌드가 영역 사용 시 사용자 확인 후 기입) |

**시전 순서(인게임):** 오염혁선13 / 풍뢰16 / 투진19 / 소명22 / 청사25 / 약영28 / 붕산31 /
비파34 / 선검37 / 혁선45 / 풍뢰50 / 투진53 / 소명56

**인게임 수치 치환 (sim 풀강화 placeholder 절대 금지):**
- 신통 계수 = 풍뢰515 / 투진485 / 소명349 / 청사374 / 약영369 / 붕산369 (아래 "신통 계수" 표)
- `CFG.신통계수보너스 / 합체기보너스 / 반허기보너스 / 인간계보너스` = 전부 **0**
- 신통 옵션값 = 인게임 max tier (아래 "신통 옵션 값" 절 — sim SK 에 적용 완료)
- 기준 스탯 = DEFAULT_STAT (baseATK 2.52억 등 인게임 제공값 — 2026-05-18 갱신)
  ⚠️ 2026-05-18 인게임 스탯 변경됨 → 아래 "60s 데미지 데이터"·방어차감 0.2167 은 **구 스탯 기준이라 stale.**
  새 스탯으로 인게임 재측정한 대조 데이터를 받아야 calibration 재개 가능.

**방어법보 호신강기:** 식혼마주·진이 마지막 장착(명공현주) 흡수 → 남은 효과 호신강기 = 107.34억
  (인게임, 사용자 확인 2026-05-17). sim DEFENSE_TREASURES shield = 53.67억/개 (107.34÷2).
  식혼 흡수 = 명공현주 → 명공현주 "미파괴 시 신통피해 +8%" passive 비활성.

## 인게임 대조 결과 (2026-05-17, 노크리 — 비술·유뢰discrete·호신강기53.67 정상 셋업)
| cast | 타격 | sim | 인게임 | 오차 |
|------|------|-----|--------|------|
| 13s 오염혁선 | 호신강기 | 77.24 | 78.48 | −1.6% |
| 16s 풍뢰 per-hit | 호신강기 | 3.11 | 2.99 | +4.0% |
| 19s 투진 hit1 | 혼합 | 20.08 | 19.08 | +5.2% |
| 22s 소명 hit1 | 체력 | 23.72 | 23.45 | +1.2% |
→ 전 cast ±5.2% 안. 잔여 +4~5%(호신강기 타격) 는 미해결 — 보호막Inc 30 or 경화령도 stack 해석 의심.

### 버그 수정 이력 (2026-05-17)
- **경화령도 호신강기 신통감소 stack** — `get경화령도Stack` 이 `_첫신통시각`(첫 신통) 기준이었음.
  사양은 "신통 **또는 법보**" → `_첫시전시각`(첫 신통/법보 = 첫 cast) 기준으로 수정. 풍뢰 +9.4%→+4.0%.
  `_첫시전시각` 은 비술_발동 함수 진입부(매 cast)에서 set.
- **방어감쇄M(=1−effDEF/K) 가설 폐기** — 풍뢰 +24% 착시의 원인은 유뢰법체Final 기댓값 오염이었지 방어 아님.
  단 "방어력 자체가 데미지에 안 들어간다"는 결론은 **아님** — 적 방어력 데미지 감소는 미해결 (위 1번 참조).
  (비술·시전순서는 위 "🧪 시뮬 셋업" 표로 이동)

## 신통 계수 (인게임 — sim 풀강화값 아님)
| 신통 | 인게임 계수 | 타수 |
|------|------|------|
| 풍뢰 | **515%** | 5회 균등 (recordEvenHit) |
| 투진 | **485%** | 7회 매번 (recordMultiHit) |
| 소명 | **349%** | 7회 |
| 청사 | **374%** | 5회 |
| 약영 | **369%** | 단일 |
| 붕산 | **369%** | 단일 |

⚠️ sim 의 `CFG.신통계수보너스(200)` + `합체기/반허기/인간계보너스` 는 풀강화 placeholder.
→ 내부 테스트엔 **이 4개 CFG 를 0 으로**, 신통 main 을 위 인게임 계수로 직접 설정.

## 신통 옵션 값 (인게임 max tier) — ✅ sim SK 정의에 적용 완료 (2026-05-17)
뇌벌(천뢰 45% / atk +25%) · 천적(계수 +4% / +2회) · 경정(11.25%) · 순요(atk +15%)
성류(최종cr +20% / 피해 +12.5%) · 천칙(inc +17.5%) · 명향(지속 +5초) · 운청(2중첩당 +5%) · 명뢰(최종cr +20%)
- sim2.js SK 적용값: 풍뢰 뇌벌 atk25·천뢰45·풍뢰남은12(천적+2) / 소명 천칙 inc17.5·성류 finalCR20·옥추4피해12.5 /
  청사 운청 5/2중첩·명뢰 localFinalCR20 / 투진 순요 atk15·경정 천뢰11.25.
- ⚠️ **자기 신통의 atk/inc 옵션 버프는 본 신통부터 적용** (사용자 지시 2026-05-17). 같은 cast 의 다단 hit 가
  명중·crit 으로 옵션 트리거 → 본 신통 hit 들에 반영. 예: 투진 SK 가 recordMultiHit 전에 명소(atk20)·순요(atk15)
  applyBuff → 투진 본 피해부터 적용. 검증: 투진 19s 노치명 −21.5% → **+0.9%**.
- 옵션 트리거 = crit 조건이면 실제 치명타율로 자연 굴림 (노치명 측정이라고 0/false 막지 말 것 — 재발 금지).
- 명소(투진 atk+20)는 인게임 옵션값 목록엔 없지만 sim placeholder 20 유지 (사용자 미지정).

## ⚠️ 법보 데미지 모드 — `CFG.법보인게임수치` (2026-05-20)
- **기본값 false = 풀강 진원-비례 공식** (TREASURE_DESCS 원문 계수). 일반 시뮬·앱·rank.js 전부 풀강.
  - 본 피해 = `진원 × 160.30% + 10억` / 호신강기 추가 = `진원 × 130.65%`
  - 참원선검 ② 호신강기무시추가 = `최대공격력 × 641.55%`
  - 6개 법보 전부 동일 계수 (환음요탑/유리옥호/산하옥척/오염혁선/경몽비파/참원선검).
- **true = 인게임 +150 calibration 수치** (하드코딩). **cal.js 만** 이걸 set (인게임 대조 정확도).
  - 오염혁선 본 43.40억 / 호신강기 27.73억 · 경몽비파 본 40.85억 / 호신강기 25.60억
  - 참원선검 ① 본 40.85억 / ② 호신강기무시 15.70억

## ⚠️ 법보 데미지 식 확정 (2026-05-21)

13s 1번 위치 4점 데이터 (4 법보 hit) 로 derive 후 검증 완료 (±1.2% fit):

### 식 구조
```
법보 절대값 공식:
  attackerDmg_pre = (base × defMult × 심화감면M + 위능) × 증가감소M × cMult × fmM

  심화감면M = 1 + (자기심화_법보 + 자기법체유뢰 − 받는감면_법보 − 받는법체유뢰)/100
  증가감소M = 1 + (보호막Inc + dealt − 영혼불씨 − defDebuff)/100
             ※ 호신강기 추가 piece (shieldOnly): 영혼불씨 미적용 (_noBulSeo)
             ※ 보호막Inc 는 shieldRem > 0 && !bypassShield 인 경우만

[추가 1] 호신강기 hit (!bypassShield) — atk/def 보너스:
  attackerDmg = attackerDmg_pre × (1 + (baseATK − baseDEF)/(baseATK + baseDEF))
  → 미러 (atk 2.56, def 2.08) → +10.34% 부스트

[추가 2] 공격력-base bypassShield piece (참원선검 ② 무시):
  attackerDmg = max(0, attackerDmg_pre − baseDEF) × (baseDEF / baseATK)
  → mirror: (sim 13.07 − 2.08) × 0.8125 = 8.93 (정확히 일치)
```

### 위치별 적용 분기

| hit 타입 | 보호막Inc | 영혼불씨 | atk/def 보너스 | atk-base 방어차감 |
|---|---|---|---|---|
| 본 piece, shield 있을 때 (호신강기 hit) | ✅ | ✅ −25 | ✅ +10.34% | ❌ |
| 본 piece, shield 0 (overflow HP) | ❌ | ✅ −25 | ✅ +10.34%* | ❌ |
| 호신강기 추가 piece (shieldOnly) | ✅ | ❌ (_noBulSeo) | ✅ +10.34% | ❌ |
| bypassShield (검 ② 무시, atk-base) | ❌ | ✅ −25 | ❌ (bypassShield 조건) | ✅ Hyp 2 |

* overflow HP-hit 도 atk/def 보너스는 받음 (현재 sim 동작 — 45s 오염혁선 검증으로 +0.5%)

### 13s 1번 검증 (4점)

| 데이터 | sim | 인게임 | 오차 |
|---|---|---|---|
| 오/비 호신강기 hit | 77.40 | 76.47 | +1.2% |
| 검 ① 본 호신강기 hit | 44.97 | 45.50 | −1.2% |
| 검 ② 무시 HP hit (atk-base bypassShield) | 8.93 | 8.93 | 0.0% ✓ exact |

### sim2.js 위치

- `dealDamage` 법보절대 분기 (line ~1683-): atk/def +10.34% 보너스 (`!opts.bypassShield` 분기)
- 같은 위치: Hyp 2 방어차감 (`opts._atkBased && opts.bypassShield` 분기)
- TREASURES.참원선검: ② 무시 piece 에 `_atkBased: true` 옵션 추가

## 법보 원문 피해값 (인게임 +150 — cal.js calibration 전용)
| 법보 | 본 피해 | 호신강기 추가 |
|------|------|------|
| 오염혁선 | 42.73억 | 27.17억 |
| 경몽비파 | 39.97억 | 25.08억 |
| 참원선검 | 39.91억 | 15.49억 (호신강기 무시 → 생명 직접) |
- 오염혁선 효과: 자신이 보호막에 가하는 피해 +15% (8초). 보호막 파괴 시 다음 법보 확정 크리.
- 참원선검 효과: 생명에 피해 시 최종피해 +5~15% (HP 낮을수록↑) — 1타만.
- 경몽비파 음역(音域): **구현됨 (2026-05-21 갱신)**. 신통/법보 시전 시 +1 누적(`state.음역`),
  경몽비파가 피해를 줄 때 소비·0 초기화. ⚠️ **비파 자기 cast 는 +1 안 함** (즉시 소비·reset 되니 net 0).
  `비술_트리거검사` 에서 `state._currentTreasure !== '경몽비파'` 일 때만 +1 (사용자 확정 2026-05-21).
  각 음역 층 → 경몽비파 `cd +12` (flat). 비파 1번 setup 에서 8 cast 사이 → 비파 시전 시 음역 8 → cd 363, cMult 1.3163.
  ⚠️ 13s 오염혁선이 성해천경 파괴 → 다음 법보(비파) 확정크리 → 비파 34s 는 항상 확정크리.

### 참원선검 = 2-hit 구조 (사용자 검증 2026-05-19, 순서 1번=13s 테스트)
sim2.js TREASURES.참원선검 — 1개 record 합산 → **2개 record 분리** 로 수정:
- ① **본 피해 40.85억** — 호신강기부터 정상 차감. 위능 적용 / 보호막Inc 적용.
- ② **호신강기무시 추가 15.70억** — `bypassShield` 로 생명 직접 타격.
  위능 미적용(`_noWiNeung`, 본 hit 1회만) / 보호막Inc 미적용(보호막에 가하는 피해 아님).
- ⚠️ **참원선검 효과 +5~15% = 기본 +5% 상시 + HP 낮을수록 +15% 까지** (사용자 확정 2026-05-19).
  기본 5% 는 호신강기를 때리든 생명을 때리든 무관하게 **항상 적용** → ①·② 둘 다 localFinalDmg=hpBonus.
- ⚠️ **HP 비율 선형 스케일 효과는 시체(HP 0) → 전부 기본값(base)** (사용자 확정 2026-05-20).
  `hpLowFactor` 가 `hpRem<=0` 시 0 반환 → 참원선검 +5~15%·검식 crRes 등 모든 hpLowFactor 기반 효과 공통.
  단 임계 조건(`hpBelow` — 현미/단천/관일/한광/복룡유파/영검법체 등)은 hpRatio 직접 사용 → 시체 = 조건 충족 발동 유지.
- ⚠️ **성해천경 파괴 −10% 는 cast 시작 시점 캡처값(`_법보최종감소_active`) 사용** — ① 이 cast 도중
  성해천경을 깨도 같은 cast 의 ② 엔 −10% 적용 X (다음 법보 cast 부터). dealDamage 가 live 값을
  읽던 버그 수정.
- 인게임 13s(순서1번, 노치명): 호신강기 −44.87 / HP −8.8 = **53.67억**.
  sim: 호신강기 42.28 / HP 12.85 = 55.13억 (총합 +2.7%). 호신강기/HP 분배 차이 있음
  (인게임이 호신강기 측에 더 쏠림 — 법보수호 1.80억 등 호신강기 가산 미구현 의심, 미해결).

## 🆕 신규 인게임 스탯 (2026-05-20)

| 스탯 | 인게임 값 | 효과 (인게임 툴팁) | sim 구현 |
|------|------|------|------|
| **겁규 어령** | 0 | 자기 받는 **신통** 피해 −10% base. 전투 중 겁규 어령이 적보다 1 높을 때마다 받는 신통 피해 추가 −0.3%. | **✅ 구현 (2026-05-20)** — `compute겁규어령Reduction()`, 옵션 보유 → stat 0 이어도 base −10% 상시 활성. 신통/연계/작열DoT 등 `받는_피해감소_신통` 에 가산. |
| **천마 위압** | 15 | 전투 중 천마 위압이 적 수련자보다 1 높을 때마다 자기 **최종피해 +0.1%** (max +10%). | **✅ 구현 (2026-05-20)** — `compute천마위압Increase()`, dealDamage `finalPct` 에 가산 (모든 type). 미러(자기=적)면 0, 비대칭/PvE 셋업에서 효과. |
| **성물 총 억제도** | 81300 | 전투 중 총 억제도가 적보다 **1% 초과당 +1% 법보 피해 증가**. 별무덤 유적 생산구역 억제 효과 합산. | **✅ 구현 (2026-05-20)** — `compute성물억제도법보Increase()`, dealDamage 증가합에 `성물억제도Inc` 가산 (`opts.absolute` 한정). 도의경지의 법보 버전. 미러 X. |
| **영압** | 4350 | (툴팁 미확인 — 사용자 지시 2026-05-20: 일단 미구현 유지) | 미구현 |

⚠️ **겁규 어령** ≠ 영혼의 불씨: 영혼불씨(신통/법보/법상 −25%/+0.15%per1%) vs 겁규어령(신통 only −10%/+0.3%per1%, 옵션 상시).
⚠️ **천마 위압** = 자기 최종피해 증가 (fmM 가산). 미러 X. 구현됨.
⚠️ **성물 총 억제도** = 자기 입히는 법보 피해 증가 (도의경지의 법보 버전). 미러 X. 구현됨.

## 인게임 60s 데미지 데이터 (노치명 / 치명) — 대조 기준
| 시점 | cast | 타격 | 노치명 | 치명 |
|------|------|------|------|------|
| 13s | 오염혁선 | 호신강기 | 78.48억 | 94.03억 |
| 16s | 풍뢰 (per-hit) | 호신강기 | 2.99억 | 5.44억 |
| 19s | 투진 1타 | 체력 | 19.08억 | 34.81억 |
| 22s | 소명 1타 | 체력 | 23.45억 | 48.84억 |
| 28s | 약영 | 체력 | 20.28억 | 52.29억 |
| 37s | 참원선검 | 체력 | 63.60억 | — |
| 45s | 오염혁선 | 체력 | 46.93억 | 62.73억 |
| 50s | 풍뢰 (per-hit) | 체력 | 3.99억 | 7.11억 |
(전체 cast·타수별 데이터는 세션 transcript 참조. 대상은 "듀라센", ~45s 사망 후 시체 타격.)

## 인게임 표시 주의
- **"X 처치" 인게임 표시 = 이미 죽은 캐릭 때려도 뜬다** (사용자 확정 2026-05-21).
  → 죽이는 hit / 시체 hit 둘 다 같은 메시지. hit type 구분 불가, 다른 정황(이전 cast 데미지 누적)으로 판단할 것.

---

# 📊 데미지 공식 변수 매트릭스 (사용자 요청 2026-05-17 — 강제 참조)

⚠️ 데미지 공식 손댈 때 **반드시 이 표 먼저 확인.** "이건 왜 빠졌어 / 왜 들어갔어" 재발 방지용.
새 변수·type 추가 시 이 표도 같이 갱신.

## 1. 공식 (type별, sim2.js dealDamage 기준)

```
신통:     attackerDmg = (effATK × defMult × (계수%/100) × 증가감소M × 심화감면M − 방어차감) × cMult × fmM
법보절대:  attackerDmg = (원문값(base) × defMult × 증가감소M × 심화감면M − 방어차감) × cMult × fmM
defMult = 1/(1−방깎디버프%)  — 적 방어 감소 디버프(둔검·검흔)만. 디버프 없으면 1.0.
방어차감 = baseDEF × 0.2167  (CFG.방어차감계수, baseDEF 1.96억 → 0.4247억). cMult/fmM 앞에서 flat 차감.
  재도출 (2026-05-17, 옥추유파 +5% 상시 반영 — "시뮬 셋업" 전부 포함 최종 baseline):
    풍뢰 16s per-hit 공격분(차감 전) = effATK 2.42억 × 1.03(계수103%) × 1.170(증가감소M)
      × 1.171(심화감면M) = 3.4147억.  인게임 풍뢰 16s per-hit = 2.99억.
    → 방어차감 = 3.4147 − 2.99 = 0.4247억.  계수 = 0.4247 / 1.96 = 0.2167. (풍뢰 노치명 +0.0% 검증)
  적용: 캐릭터(생명/HP) 직접 타격이면 type 무관 전부 (신통/연계 천뢰·낙뢰/평타/법보절대). 호신강기(법보 개체) 흡수분만 미적용(_isHpHit=false). 방어력은 캐릭 defensive stat이라 HP 타격에 당연히 적용 — 연계 제외 안 함 (2026-05-24 사용자 지시).
  ⚠️ 미러라 방어값이 baseDEF 1.96 하나뿐 → 0.1424 가 비례계수인지 고정상수인지 미도출.
     클린 데이터 1점(풍뢰 16s)으로만 fit — baseDEF 바꿔 재측정해야 형태 확정 가능.
  ※ 자기(공격자) 방어력 ≠ 자기 공격을 키우는 게 아님 — 위 방어차감은 "대상" 방어 (미러라 값 동일).
법상:     opts._isLawDamage — 신통과 동일 구조, 단 받는 stat·심화는 법상 전용 (아래 표)
영역/비술: opts._isLawDomain / _isBisul — 신통 구조 + 영역/비술 전용 심화·감면
연계피해:  천뢰/낙뢰/천검/작열DoT/작열폭발/염양/살혼 — _isShintongCat=true (신통과 같은 받는 stat)
평타/호무: 받는 stat 미적용, 평타는 ×0.05
```

## 2. 변수 → 공식 항 + type별 적용

| 변수 (인게임) | 공식 항 | 신통 | 법보절대 | 법상 | 영역 | 비술 | 연계(천뢰등) | 평타 |
|------|------|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| 공격력 (baseATK) | effATK | O | **X** (base=원문값) | O | O | O | O | O |
| 공격력 증가 (atk 버프) | effATK ×(1+atk%) | O | **X** (noAtkBuff) | O | O | O | O | O |
| 방어력 (baseDEF) | 방어차감 = baseDEF×0.2167, cMult 앞 flat 차감 (HP 타격만, 호신강기 흡수분 제외) | O | O | ? | ? | ? | O(천뢰등 HP타격) | O(평타 HP타격) |
| 방어력 증가 | — | 미모델 |||||||
| 방어력 감소 (둔검·검흔 등 디버프) | defMult = 1/(1−감소%) | O | **X** (absolute는 defDecreasePct 미계산→defMult 1) | O | O | O | O | O |
| 입히는 피해 증가 (dealt) | 증가감소M 증가합 | O | O | O | O | O | O | X |
| 입히는 신통 피해 증가 (shintong) | 증가감소M 증가합 | O | **X** (신통 전용) | ? | ? | ? | O(_isShintongCat) | X |
| 유형별 피해 증가 (type — 천뢰/작열 등) | 증가감소M 증가합 | 해당 type만 |||||||
| 보호막피해보너스 (보호막Inc) | 증가감소M 증가합 | 호신강기 타격 시 O |||||||
| 도의경지 (대도진의) | 증가감소M 증가합 | O (_isShintongCat) | **X** | 법상=? | | | O | X |
| 받는 피해 감소 (영혼의 불씨 −25%) | 증가감소M 감소합 | O (받는_피해감소_신통) | O (받는_피해감소_법보) | O (신통값) | ? | ? | O | X |
| 자기 피해 심화 (심화) | 심화감면M 심화합 (ampPct) | O (신통) | O (법보) | 법상 전용 | 영역 전용 | 비술 전용 | O | X |
| 받는 피해 감면 (감면) | 심화감면M 감면합 | O (받는_피해감면_신통) | O (받는_피해감면_법보) | **X** (법상 X, 사용자 검증) | 영역 | 비술 | O | X |
| 치명타율 (baseCR) | cr | O | O | O | O | O | O | O |
| 치명타 저항 (baseCRRes) | cr (baseCritRate 계산) | O | O | O | O | O | O | O |
| 치명타 배율 (cd — baseCD) | cMult | O (baseCD_신통) | O (baseCD_법보) | 신통값 | | | 차단 0 | 차단 0 |
| 치명타 차단 (차단 — baseCRBlock) | cMult | O (baseCRBlock_신통) | O (baseCRBlock_법보) | | | | 0 | 0 |
| 최종피해 (finalPct) | fmM = 1+final/100 | O | O (localFinalDmg/finalCDDmg) | O | O | O | O | — |

## 3. 속성 (술법 / 물리) 심화·감면

| type | 속성 심화·감면 적용? |
|------|------|
| 신통 | O — SK[skill].attr (술법/물리)별 `자기_피해심화_술법/물리`, `받는_피해감면_술법/물리` |
| **법보절대** | **X — 법보는 속성 무관 (사용자 확정 2026-05-17). `if(!opts.absolute)` 가드.** |
| 천뢰/천검/평타/호무 | 물리 |
| 낙뢰/작열DoT/작열폭발/염양 | 술법 |
| 살혼 | 속성 없음 (확정 피해) |

## 4. 계열 (영검/뇌전/화염) · 법체 심화·감면

### ⚠️ 법체 (法體) 정의 — 같은 계열 신통 4개 = 법체 활성화 (사용자 확정 2026-05-22)
- **법체 = 같은 계열(영검/뇌전/화염/백족) 신통을 4개 이상 장착하면 활성화되는 body-law.**
  - 유뢰 법체 = **뇌전 계열 신통 4개** / 영검 법체 = 영검 4개 / 현염 법체 = 화염 4개 / 백족 법체 = 백족 4개.
  - sim: `state.catSlots[계열] >= 4` 로 판정. catSlots = 빌드의 계열별 신통 개수 합.
- **계열 → 신통 family 매핑 (검증 2026-05-22 catSlots 디버그):**
  - 청명 = 뇌전 / 옥추 = 뇌전 / 복룡 = 영검 (그 외 family 는 SK 정의 SK[name].cat 참조)
  - 예: 복룡2/청명2/옥추2 빌드 → catSlots {영검:2, **뇌전:4**} → **유뢰 법체 활성** (영검 법체는 2개라 비활성).
- **법체 효과 = 활성화 시 입히는 피해 +X% / 받는 피해 −Y%** (인게임 설명: "X 법체 활성화 시 입히는/받는 피해 증가/감소").
  - 단일 증가/감소 버킷에 들어감 (자기_법체_X = 입히는 +, 받는_법체_X = 받는 −). type 무관 (신통+법보 공통).
  - sim2.js dealDamage `law체_self/law체_recv` — catSlots≥4 게이팅. 비활성 빌드면 0.
- ⚠️ **유뢰 법체 4셋 부가효과**: "치명 시 본 신통 최종피해 +22.5%" (fmM, 신통 crit 한정 — 아래 5절).


| type | 계열 심화/감면 (`자기_피해심화_영검/뇌전/화염`) | 법체 (catSlots≥4) |
|------|------|------|
| 신통 | O — 본 cast 의 SK.cat 매칭 | O — _cat 4셋 시 |
| 법보절대 | X (_cat 없음) | 백족 4셋만 예외 적용 |

## 5. 크리 공식 (사용자 확정 2026-05-17)

- **cMult = 1 + (cd − 차단)/분모** — 법보 분모 800, 신통 분모 200
  - ⚠️ 단순 빼기 식 맞음. cd·차단 값 전부 정상 (신통 cd382.56/차단218.83, 법보 cd267/차단108.92).
    type 차이는 **분모뿐** (신통 200 / 법보 800). 인게임 3점 독립 검증 — 재논의 금지.
  - **인게임 3점 검증 (2026-05-17):**
    - 풍뢰 16s (신통): `(382.56−218.83)/200` = 0.819 → cMult 1.819 = 인게임 5.44/2.99 ✓
      → 신통 /800 불가능 (차단 음수 −272 나옴). 신통은 무조건 /200.
    - 오염혁선 13s (법보, 대상 생존): `(267−108.92)/800` = 0.1976 → cMult 1.198 = 인게임 94.03/78.48 ✓
    - 오염혁선 45s (법보, **대상=시체 → 차단 0**): `(267−0)/800` = 0.3338 → cMult 1.334 = 인게임 62.73/46.93=1.337 ✓
      → 시체는 치명타차단 0 으로 클리어됨이 이 점으로 확정. `/200+차단227` 가설은 45s 가 반증 (2.335 나옴).
  - sim2.js dealDamage: `_critDiv = type==='법보절대' ? 8 : 2` (cd_effective = 100+(cd−차단)/_critDiv)
- 유뢰법체 4셋 "치명 시 최종피해 +22.5%" → 신통 crit 의 fmM 에 +22.5 (finalPct)
- 노크리 측정: cMult=1, 유뢰법체Final=0. 크리 측정: cMult=크리배율, 유뢰 +22.5.
- ⚠️ 시체(대상 HP 0) 타격 — **영혼의 불씨·치명타 차단 미해제** (사용자 확정 2026-05-19, 신규 스탯).
  인게임: 비파 56.01(시체·치명) = 노치명 46.98 × cMult 1.198 (영혼불씨 25·법보 차단 108.92 전부 그대로).
  45s 오염혁선 46.65(시체·노치명) 도 영혼불씨 그대로 계산 일치. → 시체여도 대상 스탯 정상 적용.
  시체의 유일한 차이 = 호신강기가 없음 (법보 호신강기 추가 피해만 빠짐).
  - ⚠️ 법상(진령/봉황깃털 등)도 동일 — 시체여도 영불씨·법상감면·유뢰법체받는 등 **모든 방어 감면 정상 적용** (사용자 확정 2026-05-23: 시체 데미지 뻥튀기 방지). "다 적용". 시체 차이는 호신강기 없음뿐.
  (위 250~251 줄의 "45s 차단 0 클리어" 는 구 스탯 데이터 기반 — 신규 데이터가 반증. 크리 분모
   200/800 자체는 풍뢰16s·오염혁선13s 2점으로 여전히 유효.)

---

# 📚 buff/debuff/큐/stack 매커니즘 정리 (사양 검증 완료)

## 핵심 용어 정의

| 용어 | 정의 | 예 |
|------|------|------|
| **buff** | 자기에게 적용되는 효과 (atk/cr/cd/dealt/inc/amp 등) | 명소 atk +20%, 검심통명 inc +20% |
| **debuff** | 적에게 적용되는 효과 (defDebuff/crRes/finalDmg+ 등) | 둔검 def -25%, 영역_심연 finalDmg +20% |
| **큐 (Queue)** | buff/debuff 의 실제 누적 (FIFO 동작) | 뇌인 4큐, 검세 10큐, 화상 N큐 |
| **stack** | 트리거 발동용 누적 카운터 (우리 추상, 큐와 별개) | 뇌인 stack 4 → 천벌, 검세 stack 3 → 천검 |

### 통합 모델: 모든 buff/debuff = 큐
- maxQueue=1 = 단일 효과 (갱신 = 1↔1 FIFO 교체)
- maxQueue>1 = 누적 효과 (cap 도달 시 oldest dequeue + 새 enqueue)
- 각 element 자체 TTL (사양 명시 시) 또는 영구 보존 (TTL 명시 X)

### stack 동작 패턴 (염양 스타일 carry-over)
```
매 트리거 이벤트:
  stack += 1
  while stack >= N:
    stack -= N
    fire 효과
```
"쌓이면 비우고 쌓이면 비우고" — 큐와 별개로 누적, 임계 N 도달 시 발동.

---

## 큐 (buff/debuff) 매트릭스

### A. 유파/계열 키워드 (시스템 정의 큐)
| 큐 | TTL | cap | 효과 | 소속 |
|----|-----|-----|------|------|
| **뇌인** | 20s 개별 (사용자 검증) | 4 FIFO | cr +5%/큐 | 청명 유파 |
| **검세** | 20s 개별 (사용자 검증) | 10 FIFO | amp +1.5%/큐 | 균천 유파 |
| **옥추** | 20s 개별 (사용자 검증) | 10 FIFO | inc +1%/큐 | 옥추 유파 |
  ↳ **옥추유파 +2.5%/신통** (옥추 유파 신통 N개 ×2.5) — 사양 "옥추 효과 보유 시" =
    옥추 **유파** 효과 보유(= 옥추 유파 신통 장착) 시 **상시 적용. 옥추 큐 0 이어도 적용**
    (큐 조건 X — 사용자 확정 2026-05-17). sim2.js sumShintongInc/AmpExtras/snap 전부 큐 조건 제거.
  ↳ 누적: "임의 신통으로 적에게 **치명타** 시 옥추 +1" (신통_정리.md).
    ⚠️ 멀티히트(중복타수=반사) 신통 — **hit 마다 독립 크리 판정 → 크리 hit 1회당 옥추 +1**.
    예: 투진 7타 전부 크리 → 옥추 7개 (사용자 확정 2026-05-17).
    ⚠️ 옥추 획득은 post-cast (본 cast 데미지 들어간 뒤) → **본 cast 자신엔 영향 없음**.
    sim2.js — randomCrit: hit별 실크리 굴림 후 크리 수만큼 +1. 비랜덤(랭킹): hits×crEff 분수 누적.
| **신소** | 20s 개별 (사용자 검증) | ∞ | dealt +N%/큐 | 신소 유파 |
| **검심** | **TTL 없음** (사양 명시 X) | ∞ | dealt +N%/큐 | 참허 유파 |
| **작열** | 20s 개별 (사양: "1중첩당 별도") | ∞ | DoT (각 큐 별도 데미지) | 화염 계열 공통 |

### B. 법체 부수 효과
| 큐 | TTL | cap | 효과 | 소속 |
|----|-----|-----|------|------|
| **화상** | 20s | ∞ (사양 cap 명시 X) | def -2%/큐 | 현염법체 2+ |
| **계약** (4 type) | 20s shared (사양: "20초간") | 5/type | def/cr/inc/amp +6%/큐 | 백족법체 만고귀종 |
| **독고** (4 type) | 20s shared (사양: "20초간") | 5/type | def/cr/inc/amp -2.5%/큐 | 백족법체 (격발 대상) |

### C. 불씨 세트
| 큐 | TTL | cap | 효과 |
|----|-----|-----|------|
| **진마성화** | **TTL 없음** (사양 명시 X) | 10 reject | amp +N%/큐 (3개 +1%, 6개 +3%) |

### D. 법상 시스템 (빙의 active 동안)
| 큐 | TTL | cap | 효과 | 소속 |
|----|-----|-----|------|------|
| **교혼** | 20s 개별 | 10 FIFO | atk +6%/큐 | 청교룡 |
| **염혼** | 빙의 lifetime | 누적 | 종료 시 cleanup damage | 적난새 의념 |
| **염백** | 20s 개별 | 20 FIFO | atk +1%/큐, 받는 -1%/큐 | 금오 의념 |
| **용의 예가** | 20s 개별 | 5 FIFO | 적 받는 +5%/큐, 입히는 -5%/큐 | 청룡 실체 |
| **용의 위엄** | 20s 개별 | 5 FIFO | cr +3%/큐 | 청룡 진령 |
| **적혼** | 20s 개별 | 5 FIFO | dealt +5%/큐, 받는 -5%/큐 | 주작 실체 |
| **진룡 각인** | 20s 개별 | 5 FIFO | dealt +5%/큐, 받는 -5%/큐 | 진룡 실체 |
| **봉황 각인** | 20s 개별 | 5 FIFO | 적 받는 +5%/큐, 입히는 -5%/큐 | 봉황 실체 |

### E. 신통 옵션 buff/debuff (applyBuff)
모두 사양 명시된 TTL/cap 으로 동일 매커니즘.

#### 자기 buff (예시)
| 큐 | TTL | cap | 효과 |
|----|-----|-----|------|
| 명소 | 5s | 1 | atk +20% |
| 순요 | 5s | 1 | atk +25% |
| 환우 | 10s | 1 | cr +20% |
| 뇌벌 | 10s | 1 | atk +30% |
| 종식 | 10s | 1 | atk +30% |
| 통찰 | 15s | 1 | cr +30% |
| 양운 | 5s | 5 | atk +15%/큐 |
| 겁염 | 5s | 5 | atk +8%/큐 |
| 여진 | 10s | 5 | atk +12%/큐 |
| 마찰 | 15s | 3 | cr +15%/큐 |
| 검심통명 | 10s | 1 | inc +20% |
| 검망 | (이번 천검) | 6 | 천검 dealt +40% |

#### 적 debuff (예시)
| 큐 | TTL | cap | 효과 |
|----|-----|-----|------|
| 둔검 | 10s | 1 | defDebuff -25~35% |
| 파세 | 15s | 1 | crRes -30% |
| 검흔 | 10s | 1 | defDebuff -30% |
| 절진 | 10s | 1 | crRes -20% |
| 귀진 | 10s | 1 | defDebuff -20% |
| 붕연 | 5s | 5 | defDebuff -8%/큐 |
| 동허 | 10s | 5 | defDebuff -7%/큐 |
| 굉명 | 10s | 3 | defDebuff -20% |

### F. 영역 (법칙)
| 큐 | TTL | cap | 종류 |
|----|-----|-----|------|
| 영역_열반_조건부 (HP<70 시 inc +10%) | 10s | 1 | 자기 buff |
| 영역_심연_받는피해 (적 받는 +20%) | 10s | 1 | 적 debuff |

### G. 비술 (마주)
| 큐 | TTL | cap | 효과 |
|----|-----|-----|------|
| 분혼 봉인 | 10~15s | n/a | 적 봉인 |
| 분혼 호신강기 심화 | 10s | 1 | 자기 buff |
| 식혼·진 cr buff | 140s | 1 | cr +9~19% |
| 악신 분신 | 15s | 1 | (분신 시뮬) |
| 혼원 신통차단 | 12s | 1 | 적 debuff |
| 업화 멸신 | 10s | 1 | 자기 buff |

---

## stack (트리거 카운터) 매트릭스

| stack | 임계 N | 트리거 이벤트 | 발동 효과 |
|------|------|------------|---------|
| **뇌인 stack** (`뇌인_누적`) | 4 | 신통 명중 + 영역대결 시작 | **천벌 발동** (10s 천뢰 효과) |
| **검세 stack** (`검세_누적`) | 3 | 신통 명중 + 영역대결 시작 | **천검 발동** |
| **진룡 각인 stack** (`법상_진룡각인_누적`) | 3 | 빙의 중 신통/법보 명중 | **용의 숨결 700%** 광역 |
| **작열부여 stack** (`작열부여_누적`) | 6 | 작열 부여 (열산 유파 active) | **열산상태 buff (10s amp +10%) + 염양 발동 (80%×슬롯)** |
| **영역 cast stack** (`영역_누적`) | 10 (CD 180s) | cast (신통/법보) | **영역 발동** |
| **악신·진 cast stack** | 4 | 신통/법보 cast | 분신 발동 |
| **악신·허 cast stack** | 5 | 신통/법보 cast | 분신 발동 |
| **업화 누적공격** | 5 | 신통/법보 명중 | 업화 DoT |
| **검영 히트** | 5 | 피해 5회 입힘 | 8% 호무 (max 20회) |
| **동허 히트** | 5 | 피해 5회 입힘 | def -7% buff |
| **열천 히트** | 10 | 피해 10회 입힘 | 효과 발동 |
| **진룡 공격카운터** | 2 | 빙의 중 cast | 청령 250% (의념) |
| **응현 발동** | 1 | 검심 획득 | atk +8% buff (max 5회 발동) |

### stack 의 특징
- **buff/debuff 의 큐 와 무관** — 큐가 dequeue 되어도 stack 은 누적 유지
- **TTL 없음** (큐 는 만료 가능, stack 은 cast 단위)
- **carry-over** — 임계 N 도달 시 -=N (소멸 X, 잔여 누적)

### TTL 없는 큐 vs stack 비교
- **TTL 없는 큐 (검심/진마성화)**: 큐 자체가 누적값 — stack 불필요
  - 검심: 큐 10 도달 → 큐 -10 dequeue → 검심통명 buff
  - 진마성화: 큐 10 도달 → 더 안 쌓임 (cap reject), 트리거 X
- **TTL 있는 큐 (뇌인/검세/진룡각인)**: 큐 가 시간 따라 줄어듦 — 별도 stack 필요

---

## 격발 시스템 (만고귀종, 백족법체)

### 동작
1. **독고 debuff** (4 type 큐) 누적 — 만고귀종 발동 시 4 type 균등 분수 분배 (eff/4 each)
2. **격발** = 독고 -2 큐 dequeue → 광역 75% + 계약 +1 큐 enqueue
3. **계약 buff** (4 type 큐) — 격발 결과로 획득

### 격발 vs 획득 명확 구분
- **독고**: 격발 **대상** (소비됨)
- **계약**: 격발의 **결과** (받음)
- 사양: "독고를 격발 및 소모하여 ... 계약을 1중첩 획득한다"

---

## ⚠️ 매커니즘 변경 전 필수 절차 (강제)

### 절차 (매 변경마다 반복)
1. **사양 원문 인용** — `신통_정리.md` 또는 `검선귀환_신통_정리.docx` 에서 해당 키워드/효과의 원문 찾기
2. **명시된 정보만 적용**:
   - TTL (지속시간) — 사양에 명시된 경우만 적용. 명시 없으면 TTL 없음.
   - Cap (최대 중첩) — 사양에 명시된 값만 사용
   - 트리거 조건 — 사양 wording 그대로 (예: "획득할 때마다" vs "도달 시" 구분)
   - 소비/획득 구분 — 격발 (소비 측) vs 획득 (받는 측) 명확히 분리
3. **추측 금지** — "다른 buff 도 그러니까 이것도 그럴 거야" 같은 일반화 추측 금지
4. **이전 변경의 맥락에 의존하지 말 것** — 매번 사양 직접 재확인

### 키워드 이름 변경/치환 작업 시 (강제)
사용자가 "X → Y" 로 이름 바꾸라고 할 때:
1. **먼저 Grep `X` 으로 모든 발생 위치 나열**
2. **각 위치마다 맥락 검토**:
   - 변경 대상 키워드 (예: 적난새 영혼) — 수정
   - 같은 글자 쓰는 다른 키워드/단어 (예: 환영혼, 영혼력 등) — 보존
3. **`replace_all: true` 금지** — 위치별로 명시적 `Edit` 사용
4. 실수로 다른 맥락이 바뀌면 데이터 무결성 손상

### 예시 (영혼 → 염혼 작업 시)
- ❌ `Edit replace_all: true` 로 "영혼" 모두 "염혼" 변경
- ✓ Grep "영혼" 으로 listing → 적난새 맥락만 식별 → 개별 Edit

### 명칭 사용 일관성 (강제)
- **버프/디버프** = 게임 내 효과 (자기 적용 / 적 적용)
- **큐 (Queue)** = 실제 중첩 슬롯 (FIFO 동작, 게임 데이터 존재)
- **스택 (stack)** = 시뮬레이터에서 추가한 트리거 카운터 (`state.X누적`)
- **격발** = 자원 소비 발동 (예: 독고 격발) / **획득** = 효과 받음 (예: 계약 획득)
- 큐와 스택을 혼동해서 부르지 말 것 (예: "중첩 = 스택" 같은 약식 표현 금지)

---

## 통합 큐 모델 구현 (applyBuff)

```js
function applyBuff(state, key, spec, dur, maxStack = 1) {
  // 모든 buff/debuff = 큐 (endTs 배열, FIFO)
  const ex = state.buffs.find(b => b.key === key && b.endT > state.t);
  const newEndT = state.t + dur + 0.001;
  if (ex) {
    if (!ex.endTs) ex.endTs = [ex.endT];
    ex.endTs.push(newEndT);  // enqueue
    while (ex.endTs.length > maxStack) ex.endTs.shift();  // FIFO dequeue at cap
    ex.stackCount = ex.endTs.length;
    ex.endT = Math.max(...ex.endTs);
    return;
  }
  state.buffs.push({ key, endTs: [newEndT], endT: newEndT, stackCount: 1, ...spec });
}

// prune (매 event 시작 시)
for (const b of state.buffs) {
  if (!b.endTs) b.endTs = [b.endT];
  b.endTs = b.endTs.filter(et => et > state.t - 0.1);
  b.stackCount = b.endTs.length;
  b.endT = b.endTs.length > 0 ? Math.max(...b.endTs) : 0;
}
state.buffs = state.buffs.filter(b => b.endT > state.t - 0.1);
```

### 핵심 원칙
1. **모든 buff/debuff = 큐 모델 통일** — maxStack 1 도 큐 (1↔1 FIFO = 갱신)
2. **각 element 자체 TTL** — 사양 명시 시 개별, 명시 X 시 영구
3. **stack 은 큐와 분리** — 트리거 카운터는 별도 변수 (state.X누적 등)

---

## 사용자 검증된 인게임 동작

### 천벌 발동 시퀀스 (영역=제왕의 정 빌드)
- **22s** (cast 4): 뇌인 stack 0→4 → **1st 천벌**
- **45s** (cast 10 + 영역대결 시작): 뇌인 stack 4 → **2nd 천벌**
- **59s** (cast 14): 뇌인 stack 4 → **3rd 천벌**

### 뇌인 큐 변화 (TTL 20s, cap 4 FIFO)
- 13~22s: 뇌인 0~4 누적 (cast 1~4)
- 25/28s: cap 4 FIFO 교체 (oldest 13/16 dequeue, 25/28 enqueue)
- 35/38/41/44s: TTL 만료로 staggered dequeue (4→3→2→1→0)

### 인게임 검증된 사실
- 뇌인 큐 TTL = 20s (사용자 인게임 관찰)
- 영역대결 시작도 "임의 신통" 으로 취급 → stack +1 트리거
- 영역대결 종료는 stack 트리거 X
- 천벌 active 10s 동안 재발동 X (CD 효과)

### 영혼의 불씨 — 받는 피해 감소 (사용자 확정, 2026-05-17) ⚠️⚠️ 재논의 절대 금지
**인게임 원문 (받는 측 관점):** "자신이 받는 신통·법보·진령혈맥(법상) 피해가 25.00% 감소한다.
자기 영혼의 불씨 강도가 상대를 1%씩 초과할 때마다 추가로 0.15% 감소한다."

확정 사실:
- **기본 25% 감소** — 미러(자기 강도 = 적 강도)에서도 **항상 25% 적용**. (0 아님!)
- 추가 scaling: 자기 강도가 적보다 1% 초과당 +0.15% (미러면 초과 0 → scaling 0 → 총 25%).
- **신통 / 법보 / 법상 피해 전부에 적용.** "신통 전용" 아님.
- `sim2.js:185 compute영혼의불씨강도Reduction()` — base 25 + scaling 으로 **이미 올바르게 구현됨. 코드 건드리지 말 것.**
- `받는_피해감소_법보 = 받는_피해감소_신통` (sim2.js:213, StatEditor.jsx:63) — **맞음.**

❌ 절대 금지 (Claude 가 이걸 3번 재논의해서 사용자 분노 — 완전 확정 사항):
- 법보 절대값에서 −25% 빼기 / `return 25` 의 base 25 를 0 으로 바꾸기 / "받는 신통 피해 감소라 법보 X" 라고 우기기.
- 데미지 공식이 인게임과 안 맞아도 **여기를 의심하지 말 것.** 영혼의 불씨는 검증 끝. 다른 데(증가/심화/계수/버프)를 봐라.

### 노크리 / 크리 측정 프로토콜 (사용자 확정, 2026-05-17) ⚠️⚠️ 재논의 절대 금지
인게임 노치명/치명 수치와 대조할 때:
- **데미지 관련은 노크리 시 크리 미적용.** 노크리 = 모든 데미지 `cMult = 1`.
  - 유뢰법체 4셋 "치명 시 본 신통 최종피해 +22.5%" 도 **데미지 관련 → 노크리엔 0 (미적용).**
  - ❌ "크리 떴다고 가정하고 +22.5% FULL 적용" 하지 말 것 (Claude 실수). ❌ `crEff×22.5` 기댓값도 금지.
- **크리(치명) 측정**: `cMult = 전체 크리 배율`, 유뢰법체 +22.5% 적용.
- **크리가 발동 조건인 별도 옵션/버프** (지속 버프 — 예: "치명 시 atk +X%" 같이 이후 cast 에 영향):
  → **실제 치명타율로 자연 굴림 (`randomCrit=true`)**. 굴림에서 발동하면 발동한 걸로 적용.
  → 노크리 측정이라고 이 옵션들까지 0/false 로 막으면 안 됨 (그것도 Claude 실수).
- ❌ **기댓값 절대 금지**: `cr% × 효과` 로 섞지 말 것. 사용자가 "기댓값 하지 말랬지" 5회+ 반복 지적함.

---

## 주요 데이터 구조

### state 변수 (sim2.js newState)
```js
// 큐 (buff/debuff)
state.buffs              // applyBuff 통합 큐 (all buffs/debuffs)
state.뇌인_stacks        // 뇌인 큐 (자체 관리, applyBuff 외)
state.검세_stacks        // 검세 큐
state.옥추_stacks        // 옥추 큐
state.신소_stacks        // 신소 큐
state.작열Arr            // 작열 큐 (DoT 별도 처리)
state.화상_stacks        // 화상 큐
state.법상_진룡각인_stacks  // 진룡 각인 큐
state.stacks.검심        // 검심 큐 (TTL 없음, 단순 카운터)
state.진마성화스택       // 진마성화 큐 (TTL 없음, cap 10 reject)
state.독고[type]         // 독고 큐 (4 type, fractional)
state.독고EndT[type]     // 독고 type별 shared TTL

// stack (트리거 카운터)
state.뇌인_누적           // 천벌 트리거
state.검세_누적           // 천검 트리거
state.법상_진룡각인_누적 // 700% 트리거
state.작열부여_누적     // 열산상태 + 염양 트리거
state.영역_누적   // 영역 트리거
state.업화_누적      // 업화 DoT 트리거

// 시간 기반 state (TTL only)
state.천벌End, state.천검End          // 효과 active 종료
state.영역_천위End, state.영역_심연End // 영역 자기 buff active
state.응현End, state.천연End, state.관일End // 옵션 발동 창
state.봉인EndT (분혼), state.악신EndT (악신)  // 비술 effect active
state.업화멸신End, state.혼원_신통차단EndT
state.식혼_atk (식혼)                   // 식혼 buff
state.법상_빙의시작T, state.법상_빙의종료T   // 법상 빙의 active
state.영역_pendingFireT                  // 영역 발동 예약
```


---

# ⚠️ UI/시뮬 디버깅 절차 (강제)

사용자가 `화면이 이상하다` `여전해` `동일하다` 라고 할 때 — **추측 금지, 직접 검증.**

## 1. 절대 하지 말 것

- ❌ `캐시 문제` `HMR 미반영` `React Refresh 안 됨` 같은 추측을 먼저 들이대지 말 것
- ❌ `Vite 재시작 / 페이지 reload 한 번만 해줘` 라고 사용자에게 떠넘기지 말 것 — Claude Preview 인스턴스 1개라 사용자 화면 = 내 화면이고, reload 하면 사용자 시뮬 결과까지 같이 사라짐
- ❌ 사용자가 같은 문제를 2번 이상 지적 → 추가 변경 시도 X. **검증 먼저.**
- ❌ `location.reload()` `preview_stop / preview_start` `vite restart` 를 명시적 요청 없이 시도하지 말 것

## 2. 검증 절차 (이 순서대로)

### Step 1 — 사용자 의도 정확히 정리
- 사양 원문 (`신통_정리.md` / 사용자 메시지) 인용
- 어떤 셀/위치/길이가 어떻게 되어야 하는지 명확히 (예: `약영의 현미 +45 → 받은 cast(붕산) 셀에만 표시 → width = 다음 cast 까지`)

### Step 2 — 디스크/Vite 코드 적용 여부 확인
```js
// preview_eval 로:
await import('/@fs/<absolute-path>/file.jsx?raw&t=' + Date.now())
  .then(m => m.default.includes('checkString'))
```

### Step 3 — React 가 실제 사용 중인 코드 확인
```js
// fiber 통해 현재 사용 중인 컴포넌트 함수 source 직접 추출
let fiber = element[Object.keys(element).find(k => k.startsWith('__reactFiber\$'))];
while (fiber) {
  if (fiber.type?.name === 'ComponentName') return fiber.type.toString().includes('checkString');
  fiber = fiber.return;
}
```

### Step 4 — 직접 시뮬 실행 (사용자 부담 없이)
```js
const m = await import('/src/engine/index.js?t=' + Date.now());
const { simulateBuild, CFG, SK } = m;
const events = [];
CFG.trace = (t, tag, msg) => events.push({ t, tag, msg });
// skillsOverride 는 객체 배열 ([{ name, fam }]):
const skills = skillNames.map(n => ({ name: n, fam: SK[n].fam }));
simulateBuild(build, treasures, null, skills, { maxTime: 70, ...opts });
CFG.trace = null;
// SNAP events 추출해서 cast 별 finalDmg/매핑 검증
```

### Step 5 — useMemo 결과 / casts 배열 직접 검사
```js
// fiber.memoizedState 의 hooks linked list 순회 → useMemo 결과 추출
let hook = fiber.memoizedState;
while (hook) {
  if (Array.isArray(hook.memoizedState) && hook.memoizedState[0]?.casts) {
    return hook.memoizedState[0].casts;  // 매핑된 snap 까지 포함
  }
  hook = hook.next;
}
```

## 3. 핵심 원칙

- **로직 자체를 먼저 의심.** 캐시/HMR 은 마지막 의심.
- 변경 후 **반드시 직접 검증** (Step 2~5) 후 사용자에게 결과 제시
- 사용자가 같은 문제 2번 지적 → 추가 변경 X, 검증 raisé
- `simulateBuild` 직접 호출 가능 — 사용자 시뮬 부담 없이 events 받아 분석
- React 컴포넌트의 `useMemo` 결과는 events deps 가 같으면 캐시 — events 가 새 reference 인지 확인
- `Claude Preview` 인스턴스는 사용자 화면과 동일. `preview_eval` 의 reload 명령은 사용자 화면도 같이 reset

## 4. emitSnapTrace 흐름 (sim2.js)

각 cast 종료 시 호출. `snap.finalDmg` 가 어디서 push 되는지 흐름:

| Push | Source | 의미 | 사용자 의도 (현재 사양) |
|------|--------|------|---------|
| `nc.finalDmg` | 부여한 cast 의 다음 nextCast 값 | 부여 직후 셀 표시 | **제거됨** (부여한 cast 셀 X) |
| `ncSnap.consumedSources` | 받은 cast 가 소비한 nextCast | 받은 cast 셀 표시 | **유지** (받은 cast 셀 ✓) |
| `ncSnap.finalDmg` (else) | 받은 cast 의 ncFinalDmg fallback | consumedSources 가 없을 때 | 제거됨 |
| `ncSnap.localFinalDmg` | 본 cast 한정 (예: 통백 +20) | 그 cast 자체 한정 buff | 제거됨 (단순화) |

## 5. CastTimelineSummary 셀 width 룰

- nextCast 류 stat (finalDmg/finalCR/finalCD): width = `다음 cast 시점까지` (= 1회 적용 후 끝)
- 시간 기반 buff (atk/cr/cd 등): width = `다음 cast 시점까지` (cast 간격)
- `다음 신통 cast` 가 아닌 `다음 cast` (법보 포함) — 받은 buff 는 그 cast 자체에 1회 적용 후 끝나므로

---

# ⚠️ system-reminder 의 ARGUMENTS / Skill 잔존 정보 처리 (강제)

세션 시작 직후 (특히 컨텍스트 압축 후) system-reminder 에 다음 형태가 포함될 수 있음:

```
<system-reminder>
The following skills were invoked EARLIER in this session
(before the conversation was compacted), not on the current turn.
They are shown here for context only ...

IMPORTANT: Do NOT re-execute these skills or perform their one-time
setup actions ... again.
The "## Input" sections below reflect the original arguments from when
each skill was first invoked — they are NOT the user's current message.

### Skill: ...
[skill 본문]
ARGUMENTS: [이전 invocation 의 인자]
</system-reminder>
```

## 절대 금지

- ❌ `ARGUMENTS:` 줄을 새 작업 지시로 해석 금지
- ❌ skill 의 one-time setup action (파일 생성, 변환, 다운로드 등) 재실행 금지
- ❌ 컨텍스트 직후 사용자 첫 메시지가 무엇인지 확인하지 않고 system-reminder 안 ARGUMENTS 만 보고 행동 시작 금지

## 강제 절차

1. 세션 시작 직후 system-reminder 안 `Skill: ... ARGUMENTS: ...` 가 보이면, 그 reminder 내부 안내 ("NOT the user's current message", "Do NOT re-execute") 를 우선 적용
2. 사용자가 **현재 turn** 에 명시적으로 입력한 메시지만 작업 대상
3. 사용자 메시지가 모호하면 — 추측해서 ARGUMENTS 를 따라가지 말고, 사용자에게 물어봄
4. 직전 컨텍스트 (summary 의 마지막 작업) 와 ARGUMENTS 가 **다른 작업** 을 가리키면 절대 ARGUMENTS 따라가지 않음

## 배경 (실제 사고 사례)

2026-05-10 세션: 청명·투진 반사 감폭 분석 중 컨텍스트 압축 발생 → 직후 system-reminder 에 docx skill 의 이전 invocation `ARGUMENTS: update 검선귀환_신통_정리.docx` 가 잔존 → Claude 가 이를 새 명령으로 해석해 docx 변환 스크립트 작성 + 글로벌 npm 패키지 설치 + docx 갱신 (콘텐츠 50% 누락 버그까지 포함). 사용자가 "머하는거야?" 로 중단 → 롤백.

= 같은 사고 재발 방지 위해 본 항목 강제 규칙으로 등재.
